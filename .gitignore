# Python
__pycache__/
*.py[cod]
*.egg-info/
.eggs/
build/
dist/

# Virtual envs
.venv/
venv/

# Local secrets (keep .env.example tracked, ignore the real .env)
.env

# Foundry deploy broadcast logs
contracts/broadcast/

# Generated market state + distilled recipe artifacts (spec §链下产物存储)
injenium_artifacts/
demo_artifacts/
*.lock

# Foundry build output (contracts/, M5)
contracts/out/
contracts/cache/
contracts/lib/
/out/
/cache/

# OS
.DS_Store

# Agent wallet exports (contain private keys — never commit)
agent-wallets-*.csv
